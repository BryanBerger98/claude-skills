# Architecture plan — <short need title>

> Generated by the `generate-workflow` skill on <YYYY-MM-DD>. Status: <draft | validated>.
> Target project: `<absolute project path>`. Need slug: `<slug>`.

## 1. Need summary

<Pain, current workflow, actors, frequency, ROI estimate, criticality, constraints, scope, success criteria — condensed from the validated need brief. ≤ 15 lines.>

## 2. Existing setup

<Only components classified reuse / extend / duplication-risk by audit-setup. Delete the table if the audit found nothing relevant, but keep the section with one line saying so.>

| Type   | Name   | Location | Classification | How the plan uses it |
| ------ | ------ | -------- | -------------- | -------------------- |
| <type> | <name> | <path>   | <reuse / extend / duplication-risk> | <one line> |

## 3. Proposed architecture

| Component | Type | Role | Model | Interacts with |
| --------- | ---- | ---- | ----- | -------------- |
| <name>    | <skill / subagent / hook / mcp / plugin / routine / ci / sdk / rule> | <one-line role> | <tier or n/a> | <other components> |

<Data-flow narrative: how one run traverses the components, from trigger to output. ≤ 10 lines.>

## 4. Rationale

<One block per component: why this component type over the alternatives, which best practice drives the design. Every block cites ≥ 1 source URL from research-docs.>

- **<component>** — <why this type, which alternative was rejected>. Source: <official doc URL>.

## 5. Build order

<Phases ordered by ROI: quick wins first. Each phase lists components, expected payoff, and rough effort.>

1. **Phase 1 — <label>**: <components>. Payoff: <expected gain>. Effort: <S / M / L>.
2. **Phase 2 — <label>**: <components>. Payoff: <expected gain>. Effort: <S / M / L>.

## 6. Handoff

<Every component from section 3 appears here exactly once, mapped per the SKILL.md handoff vocabulary.>

| Component | Builder | Invocation hint |
| --------- | ------- | --------------- |
| <name>    | <generate-skill / generate-agent / update-config / schedule / manual + doc URL> | <ready-to-paste prompt or command> |

## 7. Risks and costs

<Token-cost drivers, security and permission surface, maintenance burden, failure modes. ≤ 10 lines.>

## 8. Out of scope

<Component types considered and discarded, each with the one-line reason logged by research-docs. Prevents scope creep and re-litigating past decisions.>

- <type> — <reason>.
